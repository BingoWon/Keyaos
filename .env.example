# Keyaos — Local Development Environment
# Copy to .env and fill in your values.
# DO NOT COMMIT your actual .env file.

# Admin token for API access (same as wrangler.toml ADMIN_TOKEN)
ADMIN_TOKEN=admin

# Encryption key for storing API keys (set via: npx wrangler secret put ENCRYPTION_KEY)
# For local dev, any string works:
ENCRYPTION_KEY=local-dev-encryption-key

# Provider API keys (for research scripts only, not used by the worker)
OR_KEY=sk-or-your-key-here
ZENMUX_KEY=sk-zenmux-your-key-here
DEEPINFRA_KEY=sk-deepinfra-your-key-here
